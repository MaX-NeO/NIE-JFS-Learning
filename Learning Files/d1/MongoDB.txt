Meet Code :

hmz-wcgw-ene


Depenancies :

ES7+ React/Redux/React-Native snippets

Thunder Client

Spring Boot Extension Pack

Extension Pack for Java


MongoDB :

// DB

// Show all databases
show dbs;

// Create / Switch to database
use ecomm

// Drop current database
db.dropDatabase()

//Collections
// Show all collections in current db
db.ecomm.find()
// Create collection
db.createCollection("users")
// Drop collection
db.users.drop()

// Document
// Insert one document
db.users.insertOne({name:"hello",age:20})
// Insert many documents
db.users.insertMany([
{name:"a",age:20},
{name:"b",age:21}
])
// Find all documents
db.users.find()

// Find with condition
db.users.find({name:"a"})

// Update one document
db.users.updateOne({name:"a"}, {$set:{age:25}})

// Delete one document

db.users.deleteOne({name:"a"})
